{
  "name": "api",
  "version": "1.0.0",
  "description": "Api for managing the backend of the stock manager",
  "license": "MIT",
  "author": "Manuel Vasquez",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "fake-api": "json-server --watch database/db.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
